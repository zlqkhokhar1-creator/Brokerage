const express = require("express"); const logger = require("./utils/logger"); const app = express(); app.get("/health", (req, res) => { res.json({ status: "healthy", service: "zero-trust-security" }); }); const PORT = process.env.PORT || 3009; app.listen(PORT, () => { logger.info(`Zero-Trust Security running on port ${PORT}`); }); module.exports = app;
