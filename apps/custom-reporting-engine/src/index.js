const express = require("express"); const logger = require("./utils/logger"); const app = express(); app.get("/health", (req, res) => { res.json({ status: "healthy", service: "custom-reporting-engine" }); }); const PORT = process.env.PORT || 3008; app.listen(PORT, () => { logger.info(`Custom Reporting Engine running on port ${PORT}`); }); module.exports = app;
