const express = require("express"); const logger = require("./utils/logger"); const app = express(); app.get("/health", (req, res) => { res.json({ status: "healthy", service: "notification-system" }); }); const PORT = process.env.PORT || 3011; app.listen(PORT, () => { logger.info(`Notification System running on port ${PORT}`); }); module.exports = app;
