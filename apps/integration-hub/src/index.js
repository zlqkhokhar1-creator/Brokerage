const express = require("express"); const logger = require("./utils/logger"); const app = express(); app.get("/health", (req, res) => { res.json({ status: "healthy", service: "integration-hub" }); }); const PORT = process.env.PORT || 3010; app.listen(PORT, () => { logger.info(`Integration Hub running on port ${PORT}`); }); module.exports = app;
